import 'package:flutter/material.dart';

class ChannelsConfigScreen extends StatelessWidget {
  const ChannelsConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Channels Configuration - Coming Soon'),
    );
  }
}
