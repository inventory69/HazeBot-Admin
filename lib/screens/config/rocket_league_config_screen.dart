import 'package:flutter/material.dart';

class RocketLeagueConfigScreen extends StatelessWidget {
  const RocketLeagueConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Rocket League Configuration - Coming Soon'),
    );
  }
}
