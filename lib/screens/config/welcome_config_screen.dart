import 'package:flutter/material.dart';

class WelcomeConfigScreen extends StatelessWidget {
  const WelcomeConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome Configuration - Coming Soon'),
    );
  }
}
