import 'package:flutter/material.dart';

class RolesConfigScreen extends StatelessWidget {
  const RolesConfigScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Roles Configuration - Coming Soon'),
    );
  }
}
